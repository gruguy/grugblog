-- ============================================\n-- 添加音乐表scores字段\n-- ============================================\n-- 为music表添加scores字段，用于存储乐谱文件URL数组\n\n-- 直接执行 ALTER TABLE 命令添加 scores 字段\n-- 因为已经在连接配置中指定了数据库，所以不需要 USE 语句\nALTER TABLE `music` ADD COLUMN IF NOT EXISTS `scores` JSON DEFAULT NULL COMMENT "乐谱文件URL数组";\n\n-- 查看修改后的表结构\nSELECT COLUMN_NAME, DATA_TYPE, COLUMN_COMMENT\nFROM INFORMATION_SCHEMA.COLUMNS\nWHERE TABLE_NAME = 'music'\nAND COLUMN_NAME = 'scores';\n\n-- ============================================\n-- 脚本执行完成\n-- ============================================\n